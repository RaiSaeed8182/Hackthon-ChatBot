# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# MySQL Database Configuration (Optional - app works in demo mode without it)
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password_here
MYSQL_HOST=localhost
MYSQL_DATABASE=HospitalManagementSystem

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. For Streamlit Cloud deployment, set these in your app's secrets instead of .env file
# 4. If MYSQL_PASSWORD is not set, the app will run in demo mode with sample responses
